'use client';

// This component is no longer used — auth protection is handled in src/app/(app)/layout.tsx
// Kept as a stub to avoid breaking any stray imports.
export const ProtectedRoute = () => null;
